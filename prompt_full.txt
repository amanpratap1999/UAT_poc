<USER_REQUEST>
You are working on the ServiceNow UAT Agent repository located at:

C:\Users\Prakhar Singh\Desktop\UAT_Servicenow\UAT_poc

Implement a first-class LOCAL runtime while preserving the existing DOCKER runtime. Do not delete, replace, or break Docker support.

The goal is that the application can run completely on the host machine:

- FastAPI API locally;
- Celery worker locally;
- React/Vite frontend locally;
- PostgreSQL locally;
- Redis locally;
- Playwright locally with a visible browser;
- reports and screenshots written to the local repository;
- all existing Docker commands remain functional.

Do not run Docker while validating the local mode. Do not reset the repository, delete user changes, or overwrite secrets.

First inspect the complete repository and create an implementation plan. Then implement the plan. Do not stop after writing the plan. Continue making changes, testing, debugging, and correcting failures until all acceptance criteria are satisfied.

## 1. Preserve two explicit runtime modes

Add two clearly documented modes:

```text
LOCAL
DOCKER
```

Add a configuration value such as:

```text
UAT_RUNTIME_MODE=local
```

The runtime mode must not change the business logic. It should only control:

- service URLs;
- filesystem paths;
- process startup;
- frontend API routing;
- worker startup behavior;
- browser execution defaults.

The same application code must run in both modes.

## 2. Add separate environment templates

Create:

```text
.env.local.example
.env.docker.example
```

Keep `.env.example` as a common reference without real credentials.

### Local environment requirements

The local template must use:

```env
UAT_RUNTIME_MODE=local

DATABASE_URL=postgresql+asyncpg://postgres:postgres@127.0.0.1:5432/servicenow_qa
REDIS_URL=redis://127.0.0.1:6379/0
CELERY_BROKER_URL=redis://127.0.0.1:6379/0
CELERY_RESULT_BACKEND=redis://127.0.0.1:6379/1

REPORT_OUTPUT_DIR=./reports
SCREENSHOT_DIR=./screenshots

BROWSER_HEADLESS=false
BROWSER_SLOW_MO=250
BROWSER_SHOW_MOUSE_CURSOR=true
BROWSER_KEEP_BROWSER_OPEN=true
BROWSER_INTERACTIVE_TIMEOUT_SECONDS=300

VITE_API_BASE_URL=
VITE_DEV_API_PROXY_TARGET=http://127.0.0.1:8000
```

The local configuration must also include the existing ServiceNow, LLM, JWT, and perception values.

### Docker environment requirements

The Docker template must use service names:

```env
UAT_RUNTIME_MODE=docker

DATABASE_URL=postgresql+asyncpg://postgres:postgrespassword@db:5432/servicenow_qa
REDIS_URL=redis://redis:6379/0
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/1

REPORT_OUTPUT_DIR=/app/reports
SCREENSHOT_DIR=/app/screenshots
```

Docker must remain independently configurable.

Never commit real credentials.

## 3. Make environment loading reliable

The current Pydantic configuration reads `.env` by default. Add a reliable environment-file mechanism so local scripts can use `.env.local` and Docker can use `.env.docker`.

Support either:

```text
UAT_ENV_FILE=.env.local
```

or an equivalent safe implementation.

Requirements:

- environment variables already exported by the process must take precedence;
- `.env.local` must not be loaded inside Docker accidentally;
- `.env.docker` must not be required by local execution;
- all services must receive the same effective configuration;
- add a configuration diagnostic command that prints safe values only, never secrets.

## 4. Add local Windows startup scripts

Create PowerShell scripts under:

```text
scripts/
```

Add:

```text
bootstrap-local.ps1
check-local-services.ps1
start-local.ps1
stop-local.ps1
status-local.ps1
```

### `bootstrap-local.ps1`

It must:

- verify Python 3.11 or newer;
- verify Node.js and npm;
- create `.venv` if missing;
- install backend dependencies with `pip install -e ".[dev]"`;
- install Playwright Chromium;
- run `npm ci` in `frontend`;
- create `reports`, `screenshots`, `logs`, and `.runtime/local`;
- create `.env.local` from `.env.local.example` only when `.env.local` does not exist;
- never overwrite an existing `.env.local`.

### `check-local-services.ps1`

It must verify:

- PostgreSQL is reachable on the configured host and port;
- the configured database exists;
- PostgreSQL accepts the configured credentials;
- Redis responds to `PING`;
- the pgvector extension is available or provide a clear installation message;
- the API port is available;
- the frontend port is available;
- Playwright Chromium is installed.

If a dependency is missing, print the exact reason and how to fix it. Do not silently start Docker.

Support normal local PostgreSQL and a Redis-compatible Windows installation such as Memurai or WSL Redis. Document that PostgreSQL must have pgvector enabled.

### `start-local.ps1`

It must:

1. Load `.env.local`.
2. Set `PYTHONPATH` to the repository `src` directory.
3. Use the repository virtual environment interpreter.
4. Run database initialization with:

```powershell
python scripts/init_db.py
```

5. Start the API locally:

```powershell
python -m uvicorn agent.main:app --host 127.0.0.1 --port 8000 --reload
```

6. Start the Celery worker locally using a Windows-compatible pool:

```powershell
celery -A agent.core.celery_app worker --loglevel=info --pool=solo
```

7. Start the frontend locally:

```powershell
npm run dev -- --host 127.0.0.1 --port 5173
```

8. Wait for API health before starting dependent processes.
9. Store only the PIDs of processes created by this script.
10. Write separate logs to:

```text
logs/api.local.log
logs/worker.local.log
logs/frontend.local.log
```

11. Open or print:

```text
http://localhost:5173
```

12. Never kill unrelated Python, Node, Redis, PostgreSQL, or Docker processes.

Start API, worker, and frontend in separate visible PowerShell windows or provide a clear foreground mode.

### `stop-local.ps1`

It must stop only processes recorded by `start-local.ps1`.

It must:

- stop API;
- stop Celery worker;
- stop frontend;
- remove stale PID files;
- leave PostgreSQL and Redis running because they may be shared system services.

### `status-local.ps1`

It must display:

- API process and health;
- worker process;
- frontend process;
- Redis connectivity;
- PostgreSQL connectivity;
- configured runtime mode;
- browser mode;
- report and screenshot paths.

## 5. Fix Vite local API routing

The current frontend uses relative `/api` requests when `VITE_API_BASE_URL` is empty, but the Vite development server does not always proxy those requests to port 8000.

Update both:

```text
frontend/vite.config.ts
frontend/vite.config.js
```

For local development:

- if `VITE_API_BASE_URL` is empty;
- proxy `/api` to `VITE_DEV_API_PROXY_TARGET`;
- default the target to `http://127.0.0.1:8000`.

For Docker:

- preserve the existing nginx `/api/` proxy to the `api` service.

Do not change the existing behavior for Codespaces or external API URLs.

Add frontend tests proving:

- local relative API calls work through Vite proxy;
- external `VITE_API_BASE_URL` continues to work;
- Docker nginx routing remains unchanged.

## 6. Fix local filesystem paths

Ensure that local API and worker use the same physical directories:

```text
<repository>\reports
<repository>\screenshots
```

Ensure that:

- screenshots created by Playwright are visible to the API;
- perception JSON is visible to the API;
- reports are visible from the frontend;
- relative paths are resolved from the repository root, not the current terminal directory;
- local and Docker paths remain separate;
- screenshot serving remains authenticated and path traversal protected.

Add a diagnostic endpoint or script that reports the resolved paths safely.

## 7. Preserve headed browser behavior locally

In local mode verify:

```env
BROWSER_HEADLESS=false
BROWSER_SHOW_MOUSE_CURSOR=true
BROWSER_KEEP_BROWSER_OPEN=true
```

The local worker must:

- open a real visible Chromium window;
- show the visual cursor;
- use the configured slow motion;
- preserve the browser after a run;
- wait only up to `BROWSER_INTERACTIVE_TIMEOUT_SECONDS`;
- close cleanly after manual close or timeout;
- never change a finalized run result because the browser inspection wait ended.

Docker mode may default to headless mode because a normal Docker container may not have a graphical display. Document how to enable headed Docker mode only when a display server is available.

## 8. Preserve Docker support

Do not replace `docker-compose.yml`.

Update it only where necessary to ensure:

- API and worker receive the same environment variables;
- browser settings are configurable;
- report and screenshot volumes remain shared;
- Redis and PostgreSQL health checks are present;
- API and worker wait for healthy Redis/PostgreSQL;
- Docker still uses `db` and `redis` hostnames;
- frontend nginx still proxies `/api/` to `api:8000`;
- `docker compose config` succeeds;
- Docker mode does not inherit local `127.0.0.1` service URLs.

Add or update:

```text
.dockerignore
docker-compose.override.example.yml
```

only if useful and without changing the default Docker workflow.

## 9. Make Celery local execution reliable on Windows

Verify and correct:

- Celery broker URL;
- result backend URL;
- worker task discovery;
- `PYTHONPATH`;
- Windows-compatible worker pool;
- headed Playwright execution inside the worker;
- worker logs;
- worker shutdown;
- report and screenshot persistence.

Add a local smoke test proving that:

1. API creates a run;
2. Celery receives the task;
3. worker starts the orchestrator;
4. worker writes final status;
5. report is created;
6. perception evidence is created;
7. browser timeout does not convert the result to `failed`.

Do not automatically execute a live ServiceNow mutation in automated tests.

## 10. Add health and readiness checks

Keep `/api/v1/health` as a lightweight liveness endpoint.

Add a readiness endpoint such as:

```text
GET /api/v1/ready
```

It must check:

- PostgreSQL;
- Redis;
- required output directories;
- embedding-client initialization status.

Return a non-success response with a clear diagnostic when dependencies are unavailable.

The local startup script must wait for readiness before reporting that the system is ready.

## 11. Update documentation

Update `README.md` with two separate sections:

### Local execution

Include exact Windows commands:

```powershell
cd "C:\Users\Prakhar Singh\Desktop\UAT_Servicenow\UAT_poc"

.\scripts\bootstrap-local.ps1

Copy-Item .env.local.example .env.local
# Edit .env.local with real credentials

.\scripts\check-local-services.ps1
.\scripts\start-local.ps1
```

URLs:

```text
Frontend: http://localhost:5173
API:      http://localhost:8000
Health:   http://localhost:8000/api/v1/health
Ready:    http://localhost:8000/api/v1/ready
```

Stop command:

```powershell
.\scripts\stop-local.ps1
```

### Docker execution

Keep and document:

```powershell
docker compose --env-file .env.docker up --build
```

Database initialization:

```powershell
docker compose exec api python scripts/init_db.py
```

Do not mix Docker service URLs with local service URLs.

## 12. Tests and validation

Add tests for:

- local settings;
- Docker settings;
- environment precedence;
- local path resolution;
- Redis URL resolution;
- PostgreSQL URL resolution;
- Vite local proxy;
- Docker API routing;
- startup scripts;
- stop scripts;
- process PID isolation;
- readiness endpoint;
- Celery local configuration;
- headed browser local configuration.

Run all of the following:

```powershell
pytest tests -q
```

```powershell
npm test -- --run
```

```powershell
npm run type-check
```

```powershell
npm run build
```

Validate Docker configuration without starting Docker:

```powershell
docker compose --env-file .env.docker config
```

If Docker is unavailable, report that validation limitation clearly but do not remove Docker support.

## 13. Manual local acceptance test

After implementation:

1. Install local PostgreSQL with pgvector.
2. Install/start Redis or a Redis-compatible local service.
3. Run `bootstrap-local.ps1`.
4. Configure `.env.local`.
5. Run `check-local-services.ps1`.
6. Run `start-local.ps1`.
7. Open `http://localhost:5173`.
8. Log in.
9. Start an incident run.
10. Confirm the API creates the run.
11. Confirm the local Celery worker receives it.
12. Confirm a visible browser opens.
13. Confirm the cursor is visible.
14. Confirm perception screenshots load.
15. Confirm reports are written to the local `reports` directory.
16. Confirm screenshots are written to the local `screenshots` directory.
17. Confirm pause, resume, cancel, approval, and SSE events still work.
18. Confirm closing the browser does not incorrectly mark the run as failed.
19. Confirm the configured timeout closes the browser safely.
20. Stop the local application with `stop-local.ps1`.
21. Separately validate that Docker configuration still works.

Do not claim completion until both modes are preserved and the local mode passes the full acceptance test.

At the end, provide:

- exact files changed;
- exact local commands;
- exact Docker commands;
- required PostgreSQL/pgvector and Redis prerequisites;
- test results;
- any remaining limitations;
- confirmation that no Docker files or Docker functionality were removed.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-08T12:27:16+05:30.
</ADDITIONAL_METADATA>
<USER_SETTINGS_CHANGE>
The user changed setting `Model Selection` from None to Gemini 3.8 Flash (High). No need to comment on this change if the user doesn't ask about it. If reporting what model you are, please use a human readable name instead of the exact string.
</USER_SETTINGS_CHANGE>

---

<USER_REQUEST>
The local-runtime implementation appears ready for validation, but it has not yet proven that PostgreSQL, pgvector, Redis, and Playwright work correctly on your Windows machine. Antigravity mostly verified code and mocked tests.

Do not request another large implementation yet. Perform this local validation first.

### 1. Open PowerShell

```powershell
cd "C:\Users\Prakhar Singh\Desktop\UAT_Servicenow\UAT_poc"
```

Create the local environment file only if it does not already exist:

```powershell
if (-not (Test-Path ".env.local")) {
    Copy-Item ".env.local.example" ".env.local"
}
```

Open it:

```powershell
notepad ".env.local"
```

Configure the required values:

- PostgreSQL local connection
- Redis local connection
- ServiceNow URL, username, and password
- LLM provider, model, and API key
- `HEADLESS=false`
- `BROWSER_SHOW_CURSOR=true`
- `BROWSER_KEEP_OPEN=true`
- valid `JWT_SECRET_KEY`

Do not share the contents of `.env.local`.

### 2. Run the local preflight check

```powershell
.\scripts\bootstrap-local.ps1
.\scripts\check-local-services.ps1
python .\scripts\diagnose_config.py
```

The checker must confirm:

- PostgreSQL is reachable.
- PostgreSQL has the `vector`/pgvector extension.
- Redis is reachable.
- Required directories exist.
- Configuration points to `127.0.0.1`, not Docker hostnames such as `db` or `redis`.

Do not start Docker while testing local mode. Docker and local mode may cause port and configuration conflicts.

### 3. Start the application locally

```powershell
.\scripts\start-local.ps1
```

Expected services:

- Frontend: `http://localhost:5173`
- API: `http://127.0.0.1:8000`
- API documentation: `http://127.0.0.1:8000/docs`

Check readiness:

```powershell
Invoke-WebRequest "http://127.0.0.1:8000/api/v1/health"
Invoke-WebRequest "http://127.0.0.1:8000/api/v1/ready"
Invoke-WebRequest "http://127.0.0.1:8000/api/v1/diagnostics/paths"
```

Then open:

```text
http://localhost:5173
```

The browser should open as a visible headed Playwright window during an agent run, with the mouse cursor visible.

### 4. Perform the first manual test

Use the incident scenario where the expected starting state is `On Hold`, but the actual state is already `In Progress`.

The system should:

- inspect the current state;
- identify the precondition mismatch;
- stop before modifying the incident;
- mark the result as a precondition failure or blocked test;
- avoid incorrectly reporting an application defect;
- provide evidence explaining the mismatch.

Then execute a valid incident lifecycle scenario and verify:

- the browser opens visibly;
- screenshots are captured;
- the worker processes the task;
- the perception frames are visible in the UI;
- API, worker, and frontend remain connected;
- the final report contains the actual observed state transitions.

### 5. Monitor logs

```powershell
Get-Content ".\logs\api.local.log" -Wait
```

In another PowerShell window:

```powershell
Get-Content ".\logs\worker.local.log" -Wait
```

And, if available:

```powershell
Get-Content ".\logs\frontend.local.log" -Wait
```

Check process status:

```powershell
.\scripts\status-local.ps1
```

Stop everything after testing:

```powershell
.\scripts\stop-local.ps1
```

If the preflight fails, use the following prompt in Antigravity.

```text
You are responsible for completing the local-runtime validation of this repository.

Repository:
C:\Users\Prakhar Singh\Desktop\UAT_Servicenow\UAT_poc

Objective:
Make the local runtime work reliably on Windows while preserving Docker mode completely. Do not make broad architectural changes and do not rewrite working desktop-agent functionality.

Important constraints:

1. Do not run Docker containers.
2. Do not remove or break Docker Compose support.
3. Do not overwrite user secrets.
4. Do not claim success based only on mocked tests.
5. Do not modify unrelated incident-agent behavior unless it directly blocks local execution.
6. Do not execute a live mutating ServiceNow test unless explicitly required.
7. Continue diagnosing and fixing issues until the local runtime passes all acceptance criteria.
8. If a required external dependency is missing, report the exact dependency and exact installation/start command instead of pretending the task succeeded.

First inspect the actual repository and verify that the following files and behaviors really exist:

- .env.local.example
- .env.docker.example
- scripts/bootstrap-local.ps1
- scripts/check-local-services.ps1
- scripts/check_services.py
- scripts/start-local.ps1
- scripts/stop-local.ps1
- scripts/status-local.ps1
- scripts/diagnose_config.py
- src/agent/core/config.py
- src/agent/core/celery_app.py
- docker-compose.yml
- frontend/vite.config.ts
- API health and readiness routes

Validate the implementation rather than trusting documentation or a previous walkthrough.

Local-mode requirements:

1. Local mode must use host-local PostgreSQL and Redis.
2. Local mode must not resolve Docker-only hostnames such as `db`, `redis`, `api`, or `frontend`.
3. Configuration precedence must be deterministic:
   - explicitly provided process environment variables;
   - selected env file;
   - safe defaults.
4. `.env.local` must be used for local mode.
5. `.env.docker.example` must remain valid for Docker mode.
6. Local paths must resolve from the repository root.
7. Docker paths such as `/app/...` must remain unchanged for Docker.
8. Secrets must never be printed in diagnostics or logs.
9. `start-local.ps1` must pass configuration correctly to every child process.
10. `stop-local.ps1` must stop only the processes started by the local runtime and must not kill unrelated processes.
11. Startup must wait for API readiness before starting dependent components.
12. Startup failures must produce clear error messages and exit codes.
13. Celery must use a Windows-compatible worker configuration, including `--pool=solo` where required.
14. Vite must proxy API requests to the local API at `127.0.0.1:8000`.
15. The API must expose and correctly implement:
    - `/api/v1/health`
    - `/api/v1/ready`
    - `/api/v1/diagnostics/paths`

Browser requirements:

1. Local `.env` defaults must use:
   - `HEADLESS=false`
   - visible browser mode;
   - visible mouse cursor;
   - configurable slow motion;
   - configurable browser keep-open behavior.
2. Docker defaults may remain headless.
3. A real local run must open a visible browser window.
4. Playwright must not silently fall back to headless mode.
5. If the browser cannot launch, logs must show the exact Playwright error.

Run these checks:

1. Inspect the environment files without exposing secret values.
2. Run the local service preflight.
3. Verify PostgreSQL connectivity.
4. Verify the pgvector extension.
5. Verify Redis connectivity.
6. Initialize the database.
7. Start the API locally.
8. Confirm `/api/v1/health` returns HTTP 200.
9. Confirm `/api/v1/ready` returns HTTP 200 and reports all required dependencies.
10. Start the worker locally.
11. Start the frontend locally.
12. Confirm the frontend loads.
13. Confirm the frontend API proxy works.
14. Run the existing backend tests.
15. Run the existing frontend tests.
16. Run the frontend build.
17. Run a non-destructive local smoke test through the real API and worker.
18. Verify that the browser opens visibly when the smoke test reaches browser execution.
19. Verify that logs, screenshots, perception frames, and reports are written to the expected local paths.
20. Verify that Docker Compose configuration still validates using `.env.docker.example`.

If any check fails:

- identify the root cause;
- make the smallest safe code/configuration change;
- rerun the failed check;
- rerun all affected checks;
- do not stop after merely changing configuration;
- do not report “complete” until the acceptance criteria pass.

Final response must include:

1. Files changed.
2. Root cause of every failure.
3. Exact commands used.
4. Exact local prerequisites still required, if any.
5. Actual results for API, worker, frontend, browser, database, Redis, and pgvector.
6. Confirmation that Docker support remains intact.
7. Exact commands I should use for future local startup, status checking, and shutdown.
```

The most important first checkpoint is `check-local-services.ps1`. If PostgreSQL/pgvector or Redis are unavailable, the application cannot run completely locally regardless of code changes.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-08T14:20:39+05:30.
</ADDITIONAL_METADATA>

---

<USER_REQUEST>
You are responsible for completing the local-runtime validation of this repository.

Repository:
C:\Users\Prakhar Singh\Desktop\UAT_Servicenow\UAT_poc

Objective:
Make the local runtime work reliably on Windows while preserving Docker mode completely. Do not make broad architectural changes and do not rewrite working desktop-agent functionality.

Important constraints:

1. Do not run Docker containers.
2. Do not remove or break Docker Compose support.
3. Do not overwrite user secrets.
4. Do not claim success based only on mocked tests.
5. Do not modify unrelated incident-agent behavior unless it directly blocks local execution.
6. Do not execute a live mutating ServiceNow test unless explicitly required.
7. Continue diagnosing and fixing issues until the local runtime passes all acceptance criteria.
8. If a required external dependency is missing, report the exact dependency and exact installation/start command instead of pretending the task succeeded.

First inspect the actual repository and verify that the following files and behaviors really exist:

- .env.local.example
- .env.docker.example
- scripts/bootstrap-local.ps1
- scripts/check-local-services.ps1
- scripts/check_services.py
- scripts/start-local.ps1
- scripts/stop-local.ps1
- scripts/status-local.ps1
- scripts/diagnose_config.py
- src/agent/core/config.py
- src/agent/core/celery_app.py
- docker-compose.yml
- frontend/vite.config.ts
- API health and readiness routes

Validate the implementation rather than trusting documentation or a previous walkthrough.

Local-mode requirements:

1. Local mode must use host-local PostgreSQL and Redis.
2. Local mode must not resolve Docker-only hostnames such as `db`, `redis`, `api`, or `frontend`.
3. Configuration precedence must be deterministic:
   - explicitly provided process environment variables;
   - selected env file;
   - safe defaults.
4. `.env.local` must be used for local mode.
5. `.env.docker.example` must remain valid for Docker mode.
6. Local paths must resolve from the repository root.
7. Docker paths such as `/app/...` must remain unchanged for Docker.
8. Secrets must never be printed in diagnostics or logs.
9. `start-local.ps1` must pass configuration correctly to every child process.
10. `stop-local.ps1` must stop only the processes started by the local runtime and must not kill unrelated processes.
11. Startup must wait for API readiness before starting dependent components.
12. Startup failures must produce clear error messages and exit codes.
13. Celery must use a Windows-compatible worker configuration, including `--pool=solo` where required.
14. Vite must proxy API requests to the local API at `127.0.0.1:8000`.
15. The API must expose and correctly implement:
    - `/api/v1/health`
    - `/api/v1/ready`
    - `/api/v1/diagnostics/paths`

Browser requirements:

1. Local `.env` defaults must use:
   - `HEADLESS=false`
   - visible browser mode;
   - visible mouse cursor;
   - configurable slow motion;
   - configurable browser keep-open behavior.
2. Docker defaults may remain headless.
3. A real local run must open a visible browser window.
4. Playwright must not silently fall back to headless mode.
5. If the browser cannot launch, logs must show the exact Playwright error.

Run these checks:

1. Inspect the environment files without exposing secret values.
2. Run the local service preflight.
3. Verify PostgreSQL connectivity.
4. Verify the pgvector extension.
5. Verify Redis connectivity.
6. Initialize the database.
7. Start the API locally.
8. Confirm `/api/v1/health` returns HTTP 200.
9. Confirm `/api/v1/ready` returns HTTP 200 and reports all required dependencies.
10. Start the worker locally.
11. Start the frontend locally.
12. Confirm the frontend loads.
13. Confirm the frontend API proxy works.
14. Run the existing backend tests.
15. Run the existing frontend tests.
16. Run the frontend build.
17. Run a non-destructive local smoke test through the real API and worker.
18. Verify that the browser opens visibly when the smoke test reaches browser execution.
19. Verify that logs, screenshots, perception frames, and reports are written to the expected local paths.
20. Verify that Docker Compose configuration still validates using `.env.docker.example`.

If any check fails:

- identify the root cause;
- make the smallest safe code/configuration change;
- rerun the failed check;
- rerun all affected checks;
- do not stop after merely changing configuration;
- do not report “complete” until the acceptance criteria pass.

Final response must include:

1. Files changed.
2. Root cause of every failure.
3. Exact commands used.
4. Exact local prerequisites still required, if any.
5. Actual results for API, worker, frontend, browser, database, Redis, and pgvector.
6. Confirmation that Docker support remains intact.
7. Exact commands I should use for future local startup, status checking, and shutdown.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-08T14:30:00+05:30.
</ADDITIONAL_METADATA>

---

<USER_REQUEST>
Do not start Docker and do not use Docker for this validation.

I only want to verify the native local runtime on Windows.

The current verification shows:

- Local configuration passes.
- Headed Playwright launches successfully with headless=false.
- Frontend tests and build pass.
- API in-process health passes.
- PostgreSQL at 127.0.0.1:5432 is not running.
- Redis at 127.0.0.1:6379 is not running.
- API readiness correctly returns 503 because those dependencies are unavailable.

Do not modify the application architecture or switch to Docker.

Your next task is only to:

1. Verify whether native PostgreSQL exists and is running.
2. Verify whether pgvector is installed.
3. Verify whether native Redis or Memurai exists and is running.
4. Provide the exact Windows commands required to start them.
5. Do not claim the local runtime is complete until:
   - port 5432 accepts connections;
   - port 6379 accepts connections;
   - PostgreSQL has the vector extension;
   - scripts/check-local-services.ps1 passes;
   - scripts/start-local.ps1 starts API, worker, and frontend;
   - /api/v1/ready returns HTTP 200;
   - a real local run opens the headed Playwright browser.

Do not run Docker, docker compose up, or any Docker container.

If a dependency is missing, report the exact missing dependency and stop. Do not fake a successful readiness result.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-08T14:37:41+05:30.
</ADDITIONAL_METADATA>

---

<USER_REQUEST>
The report confirms that the project is not yet running completely locally.

Current situation:

- Docker has not been started.
- PostgreSQL is not installed.
- pgvector is unavailable because PostgreSQL is missing.
- Redis/Memurai is not installed.
- Ports `5432` and `6379` are closed.
- Application code, frontend, and headed Playwright are working.
- No further code changes are currently required.

You must install PostgreSQL with pgvector and Redis before running the full local application.

The simplest Docker-free route is WSL2. First check whether WSL exists:

```powershell
wsl --status
wsl -l -v
```

If Ubuntu is available, open Ubuntu and run:

```bash
sudo apt update
sudo apt install -y postgresql postgresql-contrib redis-server
sudo service postgresql start
sudo service redis-server start
```

Then check the available PostgreSQL version:

```bash
psql --version
```

Install the matching pgvector package if available:

```bash
apt-cache search pgvector
```

If a matching package is listed, install it, for example:

```bash
sudo apt install -y postgresql-16-pgvector
```

Create the database and vector extension:

```bash
sudo -u postgres createdb servicenow_qa
sudo -u postgres psql -d servicenow_qa -c "CREATE EXTENSION IF NOT EXISTS vector;"
```

Back in PowerShell, verify Windows can reach both services:

```powershell
Test-NetConnection 127.0.0.1 -Port 5432
Test-NetConnection 127.0.0.1 -Port 6379
```

Both must show:

```text
TcpTestSucceeded : True
```

Then, from the project directory:

```powershell
cd "C:\Users\Prakhar Singh\Desktop\UAT_Servicenow\UAT_poc"

.\scripts\check-local-services.ps1
.\scripts\start-local.ps1
```

Finally verify:

```powershell
Invoke-WebRequest "http://127.0.0.1:8000/api/v1/ready"
```

Only when this returns HTTP `200` is the local runtime fully ready.

Do not ask Antigravity for another implementation prompt yet. The current blocker is entirely an external dependency issue: PostgreSQL/pgvector and Redis are absent from the machine.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-08T17:34:53+05:30.
</ADDITIONAL_METADATA>

---

<USER_REQUEST>
Cant we run it locally instead of installing all these things
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-08T17:37:40+05:30.
</ADDITIONAL_METADATA>

---

<USER_REQUEST>
start the project locally i wont do anything manually
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-08T22:37:42+05:30.
</ADDITIONAL_METADATA>

---

<USER_REQUEST>
set this as LLM model 
api key :sk-SnCviquEVtJrxKdFUmFXnWpmJ0G4vfnGBDKOpqesJkaL2S09
base URL:https://api.tokenrouter.com/v1
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-09T11:44:35+05:30.
</ADDITIONAL_METADATA>

---

<USER_REQUEST>
Read-only review completed. I did not modify files or restart services.

Current local checks:

- API `/health`: `200`
- API `/ready`: `200`
- Frontend `5173`: `200`
- Local browser configuration is correctly set to headed mode:
  `BROWSER_HEADLESS=false`, `BROWSER_SHOW_MOUSE_CURSOR=true`, `BROWSER_KEEP_BROWSER_OPEN=true`

No confirmed P0 issue was found.

| Priority | Issue / RCA | Exact location | Required change |
|---|---|---|---|
| P1 | Authentication bypass. `admin/admin` is accepted even when an existing admin has another password. | [auth_router.py:36-65](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/src/agent/api/v1/auth_router.py:36>) | Remove the fallback login. Always return `401` for invalid credentials. Seed local users only through `init_db.py`. Add wrong-password tests. |
| P1 | Screenshot tenant isolation is incomplete. Any authenticated tenant can request another tenant’s screenshot if the filename is known. | [router.py:48-88](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/src/agent/api/v1/router.py:48>) | Store screenshot ownership using `run_id` and `tenant_id`. Validate ownership before returning `FileResponse`. Add cross-tenant screenshot tests. |
| P1 | Customer knowledge is not actually connected to agent execution. The orchestrator receives a new empty `CustomerKnowledgeModel`. | [dependencies.py:164-166](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/src/agent/api/v1/dependencies.py:164>), [dependencies.py:231-233](<C:/Users/Prakhar%20Singh/Desktop/UAT_Servicenow/UAT_poc/src/agent/api/v1/dependencies.py:231>), [main.py:531](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/src/agent/main.py:531>) | Run discovery per ServiceNow instance/tenant, persist the model, and inject the populated model into planning, validation, and investigation. |
| P1 | Drift detection is a stub and always reports “no drift.” | [drift.py:49-66](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/src/agent/domain/drift.py:49>), [router.py:446-459](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/src/agent/api/v1/router.py:446>) | Implement real metadata checks or display “not available.” Never report `has_drift=False` without checking ServiceNow metadata. |
| P1 | Hardcoded Incident rules can classify customer customizations as bugs. | [rules.py:23-86](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/src/agent/skills/incident/knowledge/rules.py:23>) | Use discovered instance rules as authoritative. If rules are unknown, mark the result `inconclusive`, not an application defect. |
| P1 | Defect reporting can count a raw validation mismatch as an application bug without a confirmed investigation verdict. | [engine.py:315-351](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/src/agent/reporting/engine.py:315>), [defect_scope.py:140-154](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/src/agent/domain/defect_scope.py:140>) | Make defect classification fail-closed: count a bug only when `is_defect=True` with evidence, expected value, actual value, and investigation reasoning. Otherwise classify as `agent_issue` or `inconclusive`. |
| P1 | Perception frames are written only after the run finishes, so the live perception screen remains empty during execution. Missing screenshots are also discarded completely. | [tasks.py:98-100](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/src/agent/worker/tasks.py:98>), [tasks.py:209-211](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/src/agent/worker/tasks.py:209>), [router.py:291-312](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/src/agent/api/v1/router.py:291>), [use-run-perception.ts:44-46](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/frontend/src/hooks/use-run-perception.ts:44>) | Persist/stream each frame after every action. Preserve frames without screenshots using `load_error`; do not filter them out. |
| P1 | Generated test cases are memory-only and execution sends only the title/description to Celery. The worker does not receive the structured steps. | [store.py:28-108](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/src/agent/testing/store.py:28>), [router.py:875-907](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/src/agent/api/v1/router.py:875>) | Persist test cases in PostgreSQL with tenant ownership. Queue `test_case_id`; let the worker load and execute the exact structured case. |
| P1 | Acceptance criteria are preserved as plain text but are not converted into executable assertions. | [generator.py:389-413](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/src/agent/testing/generator.py:389>), [router.py:823-829](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/src/agent/api/v1/router.py:823>) | Convert each acceptance criterion into a typed assertion or explicitly mark it unsupported/inconclusive. Add acceptance-criteria execution tests. |
| P1 | A run is committed as `queued` before Celery enqueue. If `.delay()` fails, the run remains permanently queued. | [router.py:224-244](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/src/agent/api/v1/router.py:224>) | Add an outbox/idempotent enqueue mechanism, or catch enqueue errors and mark the run failed with a visible reason. |
| P1 | SSE sends the JWT in the URL, while tokens live for 24 hours. URLs can be logged or exposed in browser history. | [auth.py:18](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/src/agent/api/v1/auth.py:18>), [LiveRunCanvas.tsx:67-69](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/frontend/src/components/runs/LiveRunCanvas.tsx:67>) | Use a short-lived SSE ticket or authenticated streaming request. Reduce JWT lifetime and redact query tokens from logs. |
| P2 | Pause/resume state is stored only in Redis, while run details are read from PostgreSQL. Refreshing the page can show stale status. | [router.py:620-685](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/src/agent/api/v1/router.py:620>) | Persist pause/resume status in `Run`; use Redis only as a worker signal. Validate legal state transitions. |
| P2 | Control events use `sequence: 0`, while normal events use an incrementing sequence. SSE can discard live pause/resume/cancel events. | [router.py:642-679](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/src/agent/api/v1/router.py:642>), [publisher.py:29-68](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/src/agent/events/publisher.py:29>) | Centralize sequence generation for every event, including control events. Add replay/live SSE tests. |
| P2 | Approval prompts are published twice: once by the orchestrator and once by `request_approval`. | [orchestrator.py:743-754](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/src/agent/cognition/orchestrator.py:743>), [publisher.py:239-240](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/src/agent/events/publisher.py:239>) | Publish exactly one prompt with one stable `prompt_id`. |
| P2 | Bounding boxes use raw screenshot coordinates against rendered image dimensions. `object-contain` letterboxing can misalign overlays. | [PerceptionOverlay.tsx:41-43](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/frontend/src/components/runs/PerceptionOverlay.tsx:41>), [PerceptionOverlay.tsx:120-137](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/frontend/src/components/runs/PerceptionOverlay.tsx:120>) | Store natural screenshot dimensions and calculate the actual displayed image rectangle before scaling boxes. |
| P2 | Screenshots intentionally hide the visual cursor by default, even though the headed browser cursor is enabled. | [manager.py:349-385](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/src/agent/browser/manager.py:349>) | Set `hide_cursor=False` for evidence screenshots when cursor visibility is required. Update the existing test that currently expects the cursor to be hidden. |
| P2 | Incident navigation has a hardcoded ServiceNow URL and does not receive configured `SERVICENOW_INSTANCE_URL`. | [skill.py:39](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/src/agent/skills/incident/skill.py:39>), [dependencies.py:138-143](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/src/agent/api/v1/dependencies.py:138>) | Pass `settings.servicenow.instance_url` into `IncidentSkill`; remove the placeholder default. |
| P2 | Local startup continues after failed readiness and does not verify worker or frontend readiness before printing success. | [start-local.ps1:148-200](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/scripts/start-local.ps1:148>) | Fail fast when `/ready` returns `503`, verify Celery worker connectivity, and poll frontend port `5173`. |
| P2 | Runtime exceptions only update the run to `failed`; the actual failure is not persisted as an agent issue or displayed in the report. | [tasks.py:293-318](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/src/agent/worker/tasks.py:293>) | Persist a sanitized runtime-error finding/event with error type, message, failed step, and traceback reference. |
| P2 | Diagnostics expose repository and filesystem paths without authentication; readiness can expose raw dependency errors. | [router.py:100-221](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/src/agent/api/v1/router.py:100>) | Require authentication for diagnostics and sanitize readiness error details. |
| P2 | CORS is configured as wildcard with credentials enabled. | [main.py:573-579](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/src/agent/main.py:573>) | Use an environment-configured frontend origin allowlist. |
| P2 | Stop script kills any process whose PID matches a stale PID file, risking termination of an unrelated process. | [stop-local.ps1:26-65](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/scripts/stop-local.ps1:26>) | Store and verify process command/path/start time before calling `taskkill`. |
| P3 | Documentation says the default database is `servicenow_uat`, while environment configuration uses `servicenow_qa`. | [README.md:64](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/README.md:64>), [.env.local.example:9](<C:/Users/Prakhar Singh/Desktop/UAT_Servicenow/UAT_poc/.env.local.example:9>) | Standardize the database name in README, `.env.example`, local setup, and Docker setup. |

Regarding the earlier `In Progress` question: the current logic correctly treats “already in the requested target state” as an idempotent pass/no-op. An explicit mismatch with a declared precondition such as “initial state must be On Hold” is correctly classified as `precondition_failed`, not an application bug. The larger unresolved risk is that unknown/custom ServiceNow behavior can still be marked as a bug because the real customer knowledge model is not wired into execution.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-09T12:39:08+05:30.
</ADDITIONAL_METADATA>

---

<USER_REQUEST>
in env change servicenow username to prakhar.s1
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-09T13:19:04+05:30.
</ADDITIONAL_METADATA>

---

<USER_REQUEST>
continue.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-09T13:21:37+05:30.
</ADDITIONAL_METADATA>
<USER_SETTINGS_CHANGE>
The user changed setting `Model Selection` from Gemini 3.8 Flash (High) to Claude Sonnet 4.6 (Thinking). No need to comment on this change if the user doesn't ask about it. If reporting what model you are, please use a human readable name instead of the exact string.
</USER_SETTINGS_CHANGE>

---

<USER_REQUEST>
check the logs and find the issue why did it failed.also one more thing as i told you that i want when the exeecution starts it should be visible live no headless browser =true it should be false.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-09T14:30:13+05:30.
</ADDITIONAL_METADATA>

---

<USER_REQUEST>
change 90 sec to 4 mins
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-09T15:50:03+05:30.
</ADDITIONAL_METADATA>

---

<USER_REQUEST>
One thing i want you to inspect in this why brwser is not opening live it displaying in task manager but doesnt opens in my screen.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-09T17:57:04+05:30.
</ADDITIONAL_METADATA>
<USER_SETTINGS_CHANGE>
The user changed setting `Model Selection` from Claude Sonnet 4.6 (Thinking) to Gemini 3.8 Flash (High). No need to comment on this change if the user doesn't ask about it. If reporting what model you are, please use a human readable name instead of the exact string.
</USER_SETTINGS_CHANGE>

---

<USER_REQUEST>
Start the server
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-10T06:43:54+05:30.
</ADDITIONAL_METADATA>

---

<USER_REQUEST>
Start the server on local not on docker
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-10T06:45:17+05:30.
</ADDITIONAL_METADATA>

---

<USER_REQUEST>
I said i want to run it locally as it was running before rather than on docker.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-10T06:57:20+05:30.
</ADDITIONAL_METADATA>

---

<USER_REQUEST>
open env local
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-10T07:07:18+05:30.
</ADDITIONAL_METADATA>

---

<USER_REQUEST>
edit admin password as admin in application authentication
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-10T07:07:54+05:30.
</ADDITIONAL_METADATA>

---

<USER_REQUEST>
you can re-intialize or update the same as i will try with new password
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-10T07:09:05+05:30.
</ADDITIONAL_METADATA>

---

<USER_REQUEST>
is all the services up and connected verify once.If any of the service is down make it up
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-10T07:18:27+05:30.
</ADDITIONAL_METADATA>

---

<USER_REQUEST>
As i told earlliler as well need to run all services locally not on docker or docker locally
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-10T07:29:33+05:30.
</ADDITIONAL_METADATA>

---

<USER_REQUEST>
where can isee the current logs 
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-10T10:40:07+05:30.
</ADDITIONAL_METADATA>

---

<USER_REQUEST>
show me in this
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-10T10:40:59+05:30.
</ADDITIONAL_METADATA>

---

<USER_REQUEST>
check the logs and its failing check wy its failing and also give me the commands to start the services locallyfrom powershell manually
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-10T10:51:20+05:30.
</ADDITIONAL_METADATA>

---

<USER_REQUEST>
stop all the services i will start manually
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-10T10:52:46+05:30.
</ADDITIONAL_METADATA>

---

<USER_REQUEST>
Change the UI of the same keeping the existing functionality same dont chnage anythig in this mae UI in light mode and also with more functionalities which you thinnk would be good for adding on and UI should look more aesthitic instead just a POC UI should be production grade product instead of the POC.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-10T12:15:02+05:30.
</ADDITIONAL_METADATA>

---

<USER_REQUEST>
restart the services
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-11T13:03:20+05:30.
</ADDITIONAL_METADATA>

---

<USER_REQUEST>
push the repo in the amanpratap1999 git
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-11T15:28:18+05:30.
</ADDITIONAL_METADATA>

---

<USER_REQUEST>
push to github
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-14T09:53:54+05:30.
</ADDITIONAL_METADATA>

---

<USER_REQUEST>
You are the principal engineer responsible for stabilizing and completing the existing
ServiceNow Autonomous UAT / Hybrid Browser Agent repository.

Your mission is to inspect the CURRENT repository, understand what is already implemented,
and then autonomously implement, test, troubleshoot, and verify the P0 → P3 work described
below.

THIS IS AN EXECUTION MANDATE, NOT A DESIGN DISCUSSION.

Do not simply tell me what should be changed.

You must:

    inspect
    → understand
    → identify root cause
    → implement
    → test
    → run
    → inspect logs
    → diagnose
    → fix
    → regression test
    → repeat

until the acceptance criteria are genuinely satisfied.

============================================================
0. ABSOLUTE RULES
============================================================

RULE 1 — DO NOT REDESIGN THE ARCHITECTURE

The existing architecture is intentional.

PRESERVE:

    NVIDIA Nemotron
        ↓
    Intent Manager
        ↓
    Cognitive Orchestrator
        ↓
    Observation Engine
        ↓
    DOM Fast Path
        ↓
    Moondream PRIMARY
        ↓
    Gemini FALLBACK
        ↓
    Action Policy
        ↓
    Playwright
        ↓
    Behavioral Verification
        ↓
    Validation Engine
        ↓
    Evidence
        ↓
    Findings
        ↓
    Reporting
        ↓
    UI

Do not replace this architecture.

Do not introduce another orchestration framework.

Do not replace Playwright.

Do not replace Nemotron.

DO NOT replace Moondream with Gemini.

Moondream MUST remain the PRIMARY visual grounding model.

Gemini MUST remain fallback / secondary capability.

DOM MUST remain the preferred deterministic path when it is reliable.

============================================================
RULE 2 — NO NEW PLUGINS
============================================================

DO NOT:

- install plugins
- change plugins
- replace plugins
- add browser extensions
- modify Antigravity plugins
- introduce external SaaS dependencies
- change the existing environment unnecessarily

Instance stability is more important than convenience.

Use the dependencies and infrastructure already present.

============================================================
RULE 3 — NO CHEATING
============================================================

Never:

- suppress errors
- delete failing assertions
- weaken validation
- lower confidence thresholds simply to obtain PASS
- manufacture findings
- hide findings
- manipulate defect counters
- mark a test PASS because the browser reached a page
- mark a test PASS because no exception was thrown
- mark a test PASS because actions > 0
- skip final verification
- convert infrastructure failures into PASS
- modify expected results to match actual results

A PASS must be earned through evidence.

============================================================
RULE 4 — INSPECT THE REPOSITORY FIRST
============================================================

Before modifying anything:

1. Inspect repository structure.
2. Inspect current git status.
3. Inspect configuration.
4. Inspect:
   - src/agent/domain
   - src/agent/intent
   - src/agent/cognition
   - src/agent/observation
   - src/agent/perception
   - src/agent/browser
   - src/agent/execution
   - src/agent/validation
   - src/agent/reporting
   - src/agent/skills
   - src/agent/worker
   - src/agent/api
   - frontend
   - tests
5. Locate existing:
   - GeneratedTestCase
   - StorySpecification
   - AcceptanceCriterion
   - IncidentSkill
   - LearningService
   - PageStateFingerprint
   - PerceptionRouter
   - PerceptionDecisionEngine
   - ReportingEngine
   - ValidationEngine
6. Inspect Docker configuration.
7. Inspect CI configuration.
8. Inspect current tests.
9. Establish the current baseline before modifying anything.

Do not assume something is missing until you verify the repository.

============================================================
RULE 5 — READ EXISTING TEST DESIGN
============================================================

The human-authored test design is the quality bar.

The agent must eventually support test cases structured around:

- Test Case ID
- User Story Reference
- Scenario
- Description
- Preconditions
- Steps
- Test Data
- Expected Result
- Test Type
- Priority
- Actual Result
- Status
- Remarks

Also preserve the richer existing internal structures if already available:

- preconditions
- test_data
- actor_role
- acceptance_criteria
- final_assertions
- risk_level

Do not throw away the existing domain model.

Extend it where necessary.

============================================================
P0 — CORE CORRECTNESS
============================================================

P0 means:

THE SYSTEM CANNOT BE CONSIDERED FUNCTIONALLY COMPLETE UNTIL THESE WORK.

------------------------------------------------------------
P0.1 REAL TEST CASE IMPORT
------------------------------------------------------------

Implement support for ingesting the existing human-authored test case format.

Inspect the repository first.

If no XLSX importer exists, implement one using the project's existing dependency strategy.

Do NOT invent a new test-case format.

The importer must map the human test-case structure into the existing domain model.

Expected conceptual mapping:

Excel Test Case
        ↓
StorySpecification / GeneratedTestCase
        ↓
Preconditions
Test Data
Ordered Steps
Expected Results
Test Type
Priority
Acceptance Criteria
Final Assertions

The imported test case must retain:

- original test case ID
- story reference
- scenario
- description
- preconditions
- steps
- test data
- expected result
- test type
- priority

No information may silently disappear during import.

Add automated tests for:

- valid workbook
- missing required columns
- malformed row
- empty test case
- multiple test cases
- duplicate test case IDs
- unsupported test type
- missing expected result

------------------------------------------------------------
P0.2 SCRIPTED TEST EXECUTION MODE
------------------------------------------------------------

When a test case already contains explicit ordered steps:

DO NOT ask the LLM to reinvent the step.

For example:

Human test case:

    Step 1:
    Open INC0000007

    Step 2:
    Verify State = On Hold

    Step 3:
    Verify Hold Reason = Awaiting Caller

    Step 4:
    Change State to In Progress

    Step 5:
    Click Update

    Step 6:
    Re-open INC0000007

    Step 7:
    Verify State = In Progress

The agent must execute those steps.

Do NOT transform them into a completely new autonomous plan.

The existing perception architecture MUST still operate underneath:

    Step
      ↓
    DOM
      ↓
    Moondream if DOM insufficient
      ↓
    Gemini if Moondream fails
      ↓
    ActionPolicy
      ↓
    Playwright
      ↓
    Verification
      ↓
    Validation

The difference is:

PLANNING is skipped when the human has already supplied the executable test intent.

This reduces unnecessary LLM calls and increases reproducibility.

------------------------------------------------------------
P0.3 STEP → ACTION CACHE
------------------------------------------------------------

Extend the existing LearningService / PageStateFingerprint mechanism.

Current learning/recovery must NOT be bypassed.

Add a safe cache concept:

    page_state_fingerprint
    +
    normalized_test_step
    +
    application/context
        ↓
    previously verified action/locator

Example:

    fingerprint = ABC123
    step = "Click Update"
        ↓
    verified selector = #sysverb_update

On the next identical execution:

    same page fingerprint
    +
    same step
        ↓
    reuse verified action

IMPORTANT:

The cache MUST NOT blindly trust stale selectors.

Before executing cached actions:

- verify page context
- verify selector still exists
- verify visibility/executability
- invalidate stale cache entries
- fall back to DOM
- then Moondream
- then Gemini

The cache is an optimization.

It must NEVER bypass safety or perception validation.

------------------------------------------------------------
P0.4 INCIDENT LIFECYCLE — PRIMARY ACCEPTANCE TEST
------------------------------------------------------------

The primary real-world acceptance test is:

INC0000007

Initial conditions:

    Incident Number:
    INC0000007

    State:
    On Hold

    Hold Reason:
    Awaiting Caller

Required actions:

    Change State → In Progress

    Click Update

Then:

    Re-open INC0000007

Final condition:

    Incident Number = INC0000007

    State = In Progress

DO NOT:

- create another incident
- close the incident
- modify unrelated fields
- change priority
- change assignment
- change description
- change caller
- change category
- execute unrelated setup actions

The test must independently verify the final persisted state.

Do not infer persistence from the pre-update page.

The agent MUST:

    open record
      ↓
    verify record identity
      ↓
    verify initial state
      ↓
    verify hold reason
      ↓
    modify state
      ↓
    update
      ↓
    reopen same record
      ↓
    independently read persisted state
      ↓
    verify In Progress

If reopening fails:

DO NOT mark PASS.

Trace and fix the navigation/root cause.

------------------------------------------------------------
P0.5 NAVIGATION IDENTITY SAFETY
------------------------------------------------------------

The previous system encountered invalid ServiceNow relative URLs.

Investigate all ServiceNow URL construction.

Never blindly call:

    page.goto(relative_url)

if Playwright requires an absolute URL.

Use the existing browser/base URL architecture.

Prefer stable record identity:

    sys_id
    incident number

over fragile list/navigation URLs whenever possible.

Verify that:

    INC0000007
        ↓
    correct sys_id
        ↓
    correct incident record

is maintained throughout the workflow.

Add regression tests around:

- absolute ServiceNow URL
- relative URL resolution
- record URL generation
- incident list URL
- incident form URL
- malformed URL
- wrong/missing base URL

------------------------------------------------------------
P0.6 FINAL VERIFICATION
------------------------------------------------------------

Final verification MUST be independent.

The final result cannot be:

    "we clicked Update successfully"

It must be:

    "we reopened INC0000007 and observed State = In Progress"

The verification must operate on fresh browser state.

If possible:

    reload/reopen
        ↓
    observe
        ↓
    verify

Do not use cached state for final assertions.

------------------------------------------------------------
P0.7 FALSE-PASS PREVENTION
------------------------------------------------------------

The following are NOT sufficient for PASS:

- state machine reached COMPLETED
- actions > 0
- defects = 0
- browser click succeeded
- no exception occurred

PASS requires:

    required steps executed
    +
    expected results verified
    +
    final assertions verified
    +
    no unresolved defects
    +
    evidence exists

If any required assertion is missing:

    NOT PASS

------------------------------------------------------------
P0.8 FINDING / DEFECT CONSISTENCY
------------------------------------------------------------

Ensure:

    Run.defect_count
    Run.findings_count
    Finding records
    TestReport
    UI

all agree.

For a genuine successful acceptance test:

    defects = 0
    findings = 0

If there is a real defect:

    persist it

Do not manipulate counters.

Deduplicate identical underlying failures.

============================================================
P1 — HIGH PRIORITY PERFORMANCE / RELIABILITY
============================================================

After P0 is stable, implement P1.

------------------------------------------------------------
P1.1 LLM RATE LIMITING
------------------------------------------------------------

Inspect the existing global LLM rate limiter.

If all providers/purposes currently share one global lock:

split logically by provider/purpose.

At minimum distinguish:

    planner
    Moondream
    Gemini
    verification

Do NOT violate provider limits.

Goal:

An unrelated planner call should not unnecessarily block a vision call.

Measure before/after latency.

------------------------------------------------------------
P1.2 SCREENSHOT DEDUPLICATION
------------------------------------------------------------

Inspect the current screenshot lifecycle.

Avoid unnecessary sequences such as:

    before_perception
    vision screenshot
    escalation screenshot
    after_perception

when the browser state has not changed.

Reuse the same screenshot where safe.

IMPORTANT:

Never reuse an image after the browser state has materially changed.

Evidence correctness is more important than saving one screenshot.

------------------------------------------------------------
P1.3 VISION CALL BUDGET
------------------------------------------------------------

Introduce an action-level model call budget.

A failed action must not explode into:

    DOM
    → Moondream
    → Gemini
    → retry
    → Moondream
    → Gemini
    → verifier
    → recovery
    → verifier
    ...

Track:

    planner calls
    Moondream calls
    Gemini calls
    verification calls

per action and per run.

Expose this telemetry in logs/reporting.

Do not silently skip required verification.

------------------------------------------------------------
P1.4 PAGE-STATE GROUNDING CACHE
------------------------------------------------------------

Extend PageStateFingerprint to cache successful first-pass grounding.

Example:

    fingerprint X
    target "Update"
        ↓
    DOM selector #sysverb_update

Next occurrence:

    fingerprint X
    target "Update"
        ↓
    reuse verified grounding

If fingerprint changes materially:

    invalidate/re-evaluate

This must remain compatible with:

    DOM → Moondream → Gemini

------------------------------------------------------------
P1.5 PROMPT EFFICIENCY
------------------------------------------------------------

Inspect repeated system prompts.

Where provider APIs support caching, use the existing supported mechanism.

Do not implement fake caching.

Reduce repeated static context where safely possible.

Do not remove important instructions just to reduce tokens.

------------------------------------------------------------
P1.6 CELERY CONCURRENCY
------------------------------------------------------------

Inspect current worker configuration.

If currently:

    --pool=solo

determine whether the runtime environment permits a safer concurrent configuration.

Do not blindly change it.

Test worker behavior first.

If concurrency is introduced:

- make it configurable
- ensure browser sessions remain isolated
- ensure one UAT run cannot affect another
- ensure database state is safe
- ensure logs remain attributable to the correct run

Do not sacrifice browser isolation for throughput.

------------------------------------------------------------
P1.7 BROWSER WARMING
------------------------------------------------------------

Investigate browser startup cost.

If safely possible within existing architecture:

reuse browser process/context where appropriate.

DO NOT share state between unrelated UAT sessions.

Authentication/session isolation must remain intact.

============================================================
P1.8 TEST DESIGN COVERAGE
============================================================

The human test design includes more than happy-path tests.

Extend the existing testing strategy taxonomy to support:

    Functional
    Boundary
    Equivalence
    Decision Table
    State Transition
    Security / RBAC
    Idempotency
    Audit / Logging
    Error Isolation
    End-to-End

Do not remove existing strategies.

Add a `test_type` or equivalent field only if the current model requires it.

Ensure the planner/importer can preserve the test type.

============================================================
P1.9 ROLE / PERSONA EXECUTION
============================================================

Inspect existing actor_role support.

Implement role-sweep capability only if it fits the existing architecture.

Example:

    Test Case
       ↓
    PM
       ↓
    Program Manager
       ↓
    PMO

For each role:

- authenticate correctly
- execute the same relevant test
- verify expected visibility/editability
- isolate browser/session state
- report results separately

Do not assume every test needs every role.

Role execution must be explicitly defined by the test case.

============================================================
P1.10 XLSX RESULT EXPORT
============================================================

The human tester works in a structured spreadsheet.

The agent must eventually be able to export execution results back into the same conceptual schema:

    Test Case ID
    User Story Ref
    Scenario
    Description
    Preconditions
    Steps
    Test Data
    Expected Result
    Test Type
    Priority
    Actual Result
    Status
    Remarks

Actual Result must contain factual execution information.

Example:

    "Incident INC0000007 was reopened after Update.
     State was observed as In Progress."

Do not output vague statements such as:

    "Test completed successfully."

============================================================
P2 — PRODUCTION HARDENING
============================================================

------------------------------------------------------------
P2.1 SECURITY CONFIGURATION
------------------------------------------------------------

Inspect JWT configuration.

If a well-known default JWT secret can be used outside local development:

fix this.

Non-local/production runtime must require an explicit secret.

Do not break intentional local development behavior.

Add startup validation.

------------------------------------------------------------
P2.2 DOCKER HARDENING
------------------------------------------------------------

Inspect Dockerfiles.

Where safe:

- non-root runtime user
- multi-stage build
- remove unnecessary build dependencies
- healthcheck
- explicit runtime configuration

Do not modify Docker architecture unnecessarily.

Run actual containers after changes.

Inspect:

    docker compose ps

and relevant logs.

------------------------------------------------------------
P2.3 OBSERVABILITY
------------------------------------------------------------

Every action should be traceable.

A useful execution chain should look conceptually like:

    run_id
      ↓
    step_id
      ↓
    action_id
      ↓
    perception
      ↓
    execution
      ↓
    verification
      ↓
    validation
      ↓
    evidence
      ↓
    finding

Use existing logging conventions where possible.

Do not create an entirely new logging architecture.

------------------------------------------------------------
P2.4 MODEL FAILURE VS PRODUCT FAILURE
------------------------------------------------------------

Separate:

    NVIDIA API failure
    Moondream failure
    Gemini failure
    browser failure
    ServiceNow application defect
    validation failure

Example:

NVIDIA returns HTTP 503.

That is NOT automatically:

    ServiceNow defect.

Likewise:

Playwright selector fails.

That is not automatically:

    ServiceNow application defect.

The reporting layer must distinguish infrastructure/model/browser problems from actual UAT defects.

------------------------------------------------------------
P2.5 NETWORK DELTA VALIDATION
------------------------------------------------------------

Apply the baseline/delta principle used for console errors.

Avoid treating unrelated pre-existing network issues as defects.

Detect genuinely new failures caused by the current action where possible.

------------------------------------------------------------
P2.6 LEARNING SAFETY
------------------------------------------------------------

Ensure learned recovery never becomes an unsafe shortcut.

Required order remains:

    DOM
      ↓
    Moondream
      ↓
    Gemini
      ↓
    learned recovery where appropriate

or the equivalent existing safe recovery design.

A learned selector must be executable and context-valid before use.

============================================================
P2.7 PERCEPTION EVIDENCE
============================================================

Ensure the actual runtime perception information survives:

    perception
       ↓
    ActionResult
       ↓
    StepEvidence
       ↓
    TestReport
       ↓
    persistence
       ↓
    API
       ↓
    UI

If DOM was used:

    route = DOM

If Moondream was used:

    provider = Moondream

If Gemini was used:

    provider = Gemini

Never fabricate perception records.

============================================================
P2.8 KNOWLEDGE MODEL
============================================================

Inspect the existing Knowledge Model implementation.

Verify whether:

    GET /api/v1/knowledge-model/rules
    GET /api/v1/knowledge-model/rules/{rule_id}
    GET /api/v1/knowledge-model/drift

are implemented and correctly connected to the existing CustomerKnowledgeModel.

If already implemented:

fix the integration/UI issue.

If genuinely missing:

implement the smallest compatible API.

Do not redesign the Knowledge Model.

Do not fabricate rules.

============================================================
P2.9 REGRESSION TESTING
============================================================

Protect the existing baseline.

Previously established baseline:

    175 passed
    7 skipped
    0 failed

If current repository has a different baseline, record the actual baseline first.

After every significant change:

    targeted tests

Then:

    relevant regression suite

At the end:

    full regression suite

Never delete or weaken tests simply because they fail after your change.

============================================================
P3 — MAINTAINABILITY / QUALITY
============================================================

------------------------------------------------------------
P3.1 DOCUMENTATION
------------------------------------------------------------

Create/update:

    docs/architecture_decision_records/

Include an ADR explaining:

    Why DOM is preferred
    Why Moondream is PRIMARY vision
    Why Gemini is fallback
    Why Playwright performs execution
    Why AI does not directly control unrestricted browser actions

------------------------------------------------------------
P3.2 CONTRIBUTING
------------------------------------------------------------

Add/update:

    CONTRIBUTING.md

Explain:

- local setup
- Docker setup
- tests
- logs
- architecture
- model configuration
- safe modification rules

------------------------------------------------------------
P3.3 TROUBLESHOOTING RUNBOOK
------------------------------------------------------------

Create:

    docs/troubleshooting.md

Cover:

    Docker failure
    browser startup failure
    Playwright selector failure
    Moondream failure
    Gemini fallback
    NVIDIA 503
    validation failure
    evidence missing
    findings mismatch
    ServiceNow navigation failure
    authentication failure

------------------------------------------------------------
P3.4 ROOT REPOSITORY HYGIENE
------------------------------------------------------------

Inspect tracked files.

Runtime artifacts such as:

    api.log
    worker.log

must not be committed.

Diagnostic scripts should be organized appropriately.

Do NOT blindly rewrite Git history.

If history rewriting would be destructive:

STOP and document the issue rather than performing a dangerous history rewrite.

============================================================
P3.5 ACTUAL RESULT NARRATIVE
============================================================

Generate a human-readable Actual Result only once per completed test case where possible.

Do not make every step invoke an LLM just to generate prose.

Actual Result should be factual.

Example:

    "INC0000007 was initially observed in On Hold with
     Hold Reason Awaiting Caller. The State was changed to
     In Progress and Update was executed. The incident was
     reopened and independently verified as In Progress."

============================================================
P3.6 COST / PERFORMANCE TELEMETRY
============================================================

Track per run:

    total actions
    DOM actions
    Moondream calls
    Gemini calls
    planner calls
    verification calls
    screenshots
    average action latency
    model latency
    total runtime

This allows future optimization based on measurements rather than guesses.

============================================================
AUTONOMOUS EXECUTION LOOP
============================================================

You are explicitly authorized to iterate.

For every discovered issue:

1. Identify symptom.
2. Find exact code path.
3. Find root cause.
4. Implement smallest correct fix.
5. Add/adjust regression test.
6. Run targeted test.
7. Run relevant integration test.
8. If runtime behavior matters:
       run the actual system.
9. Inspect logs yourself.
10. Compare actual behavior against acceptance criteria.
11. If anything fails:
       continue.
12. Repeat.

DO NOT stop merely because one layer works.

============================================================
SELF-ROAST PROTOCOL
============================================================

Before declaring anything fixed, aggressively challenge your own result.

Ask:

    Did I actually test the runtime?

    Did I only inspect the code?

    Did Playwright really perform the action?

    Did ServiceNow actually persist the change?

    Did I reopen the same record?

    Did I independently verify the final state?

    Did the expected result come from the test case?

    Did I accidentally let the planner invent extra actions?

    Did I bypass Moondream?

    Did Gemini become primary accidentally?

    Did I weaken validation?

    Did I suppress an exception?

    Did I manipulate defect counters?

    Did I make the report look better instead of making
    the system better?

    Did the UI receive real evidence?

    Could this still be a false PASS?

If the answer to any question is uncertain:

    CONTINUE.

============================================================
FINAL ACCEPTANCE TEST
============================================================

The following must be executed against the actual ServiceNow environment.

TEST:

    INC0000007 Incident Lifecycle

PRECONDITION:

    Incident exists.

INITIAL ASSERTIONS:

    Incident Number = INC0000007
    State = On Hold
    Hold Reason = Awaiting Caller

ACTION:

    Change State to In Progress
    Click Update

POST-ACTION:

    Reopen INC0000007

FINAL ASSERTIONS:

    Incident Number = INC0000007
    State = In Progress

NEGATIVE ASSERTIONS:

    No unrelated fields modified.
    No new incident created.
    Incident not closed.
    Wrong record not opened.
    No unrelated actions executed.

PERCEPTION ASSERTIONS:

    DOM used when reliable.
    Moondream used when DOM insufficient.
    Gemini only used as fallback where necessary.

VALIDATION ASSERTIONS:

    Browser action succeeded.
    Expected state transition occurred.
    Final state independently verified.
    No unexpected new JS errors.
    No unexpected action-caused network failures.
    Behavioral verification passed or valid deterministic fallback passed.

REPORTING ASSERTIONS:

    action count correct.
    finding count correct.
    defect count correct.
    evidence persisted.
    perception evidence persisted where applicable.
    final report matches actual execution.

============================================================
FINAL REGRESSION GATE
============================================================

Run the complete test suite.

Record exact:

    collected
    passed
    skipped
    failed

No unexplained regression is acceptable.

============================================================
FINAL MASTER REPORT
============================================================

Create/update ONE report:

    docs/final_autonomous_stabilization_report.md

Do not create dozens of scattered reports.

The report MUST contain:

1. Executive Summary

2. Repository Baseline

3. Current Architecture

4. P0 Work

5. P1 Work

6. P2 Work

7. P3 Work

8. Every Issue Discovered

For every issue:

    symptom
    log evidence
    root cause
    affected files
    fix
    tests
    result

9. Runtime Iteration History

    Iteration
    Problem
    Root Cause
    Fix
    Test
    Result

10. Incident Acceptance Evidence

    INC0000007
    Initial State
    Initial Hold Reason
    State Change
    Update
    Reopen
    Final State

11. Perception Evidence

    DOM
    Moondream
    Gemini

including actual confidence/coordinates/locators where available.

12. Model Usage

    Nemotron calls
    Moondream calls
    Gemini calls
    verification calls

13. Performance

    runtime
    action latency
    model latency
    screenshot count

14. Regression Results

15. Remaining Issues

16. Security Findings

17. Final Verdict

Use EXACTLY:

    PASS — ALL ACCEPTANCE CRITERIA VERIFIED

or:

    FAIL — ACCEPTANCE CRITERIA NOT FULLY VERIFIED

Never use:

    "mostly passed"
    "almost done"
    "looks good"
    "production ready"

unless the evidence genuinely supports it.

============================================================
ORDER OF EXECUTION
============================================================

Do NOT attack everything randomly.

Execute in this order:

PHASE 0
Repository inspection + baseline

        ↓

PHASE 1
P0 security/correctness blockers

        ↓

PHASE 2
Real test-case import

        ↓

PHASE 3
Scripted execution mode

        ↓

PHASE 4
INC0000007 end-to-end lifecycle

        ↓

PHASE 5
Final verification + evidence + findings

        ↓

PHASE 6
P1 performance/cost/reliability

        ↓

PHASE 7
P2 production hardening

        ↓

PHASE 8
P3 documentation/product quality

        ↓

PHASE 9
Full regression suite

        ↓

PHASE 10
Repeat failed areas until green

        ↓

FINAL MASTER REPORT

============================================================
ULTIMATE RULE
============================================================

DO NOT STOP UNTIL THE SYSTEM HAS ACTUALLY DEMONSTRATED THE TARGET.

The target is NOT:

"the code looks correct."

The target is NOT:

"tests passed."

The target is NOT:

"the UI says completed."

The target is:

    HUMAN TEST CASE
          ↓
    IMPORTED CORRECTLY
          ↓
    EXECUTED CORRECTLY
          ↓
    SERVICE NOW ACTUALLY CHANGED
          ↓
    RECORD REOPENED
          ↓
    FINAL STATE INDEPENDENTLY VERIFIED
          ↓
    EVIDENCE PERSISTED
          ↓
    FINDINGS CORRECT
          ↓
    REPORT CORRECT
          ↓
    REGRESSION SUITE GREEN
          ↓
    GENUINE PASS

Be surgical.

Be skeptical.

Be autonomous.

Read your own logs.

Fix your own mistakes.

Never hide a failure.

Never cheat the acceptance criteria.

Never change the architecture simply because an implementation is difficult.

The existing Hybrid Browser Agent architecture is the product.

Your job is to make it reliable.

you just make the implementation plan i will ask other model to follow the same.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-15T12:47:51+05:30.
</ADDITIONAL_METADATA>
<USER_SETTINGS_CHANGE>
The user changed setting `Model Selection` from Gemini 3.8 Flash (High) to Claude Opus 4.6 (Thinking). No need to comment on this change if the user doesn't ask about it. If reporting what model you are, please use a human readable name instead of the exact string.
</USER_SETTINGS_CHANGE>

---

<USER_REQUEST>
You are the principal engineer responsible for stabilizing and completing the existing
ServiceNow Autonomous UAT / Hybrid Browser Agent repository.

Your mission is to inspect the CURRENT repository, understand what is already implemented,
and then autonomously implement, test, troubleshoot, and verify the P0 → P3 work described
below.

THIS IS AN EXECUTION MANDATE, NOT A DESIGN DISCUSSION.

Do not simply tell me what should be changed.

You must:

    inspect
    → understand
    → identify root cause
    → implement
    → test
    → run
    → inspect logs
    → diagnose
    → fix
    → regression test
    → repeat

until the acceptance criteria are genuinely satisfied.

============================================================
0. ABSOLUTE RULES
============================================================

RULE 1 — DO NOT REDESIGN THE ARCHITECTURE

The existing architecture is intentional.

PRESERVE:

    NVIDIA Nemotron
        ↓
    Intent Manager
        ↓
    Cognitive Orchestrator
        ↓
    Observation Engine
        ↓
    DOM Fast Path
        ↓
    Moondream PRIMARY
        ↓
    Gemini FALLBACK
        ↓
    Action Policy
        ↓
    Playwright
        ↓
    Behavioral Verification
        ↓
    Validation Engine
        ↓
    Evidence
        ↓
    Findings
        ↓
    Reporting
        ↓
    UI

Do not replace this architecture.

Do not introduce another orchestration framework.

Do not replace Playwright.

Do not replace Nemotron.

DO NOT replace Moondream with Gemini.

Moondream MUST remain the PRIMARY visual grounding model.

Gemini MUST remain fallback / secondary capability.

DOM MUST remain the preferred deterministic path when it is reliable.

============================================================
RULE 2 — NO NEW PLUGINS
============================================================

DO NOT:

- install plugins
- change plugins
- replace plugins
- add browser extensions
- modify Antigravity plugins
- introduce external SaaS dependencies
- change the existing environment unnecessarily

Instance stability is more important than convenience.

Use the dependencies and infrastructure already present.

============================================================
RULE 3 — NO CHEATING
============================================================

Never:

- suppress errors
- delete failing assertions
- weaken validation
- lower confidence thresholds simply to obtain PASS
- manufacture findings
- hide findings
- manipulate defect counters
- mark a test PASS because the browser reached a page
- mark a test PASS because no exception was thrown
- mark a test PASS because actions > 0
- skip final verification
- convert infrastructure failures into PASS
- modify expected results to match actual results

A PASS must be earned through evidence.

============================================================
RULE 4 — INSPECT THE REPOSITORY FIRST
============================================================

Before modifying anything:

1. Inspect repository structure.
2. Inspect current git status.
3. Inspect configuration.
4. Inspect:
   - src/agent/domain
   - src/agent/intent
   - src/agent/cognition
   - src/agent/observation
   - src/agent/perception
   - src/agent/browser
   - src/agent/execution
   - src/agent/validation
   - src/agent/reporting
   - src/agent/skills
   - src/agent/worker
   - src/agent/api
   - frontend
   - tests
5. Locate existing:
   - GeneratedTestCase
   - StorySpecification
   - AcceptanceCriterion
   - IncidentSkill
   - LearningService
   - PageStateFingerprint
   - PerceptionRouter
   - PerceptionDecisionEngine
   - ReportingEngine
   - ValidationEngine
6. Inspect Docker configuration.
7. Inspect CI configuration.
8. Inspect current tests.
9. Establish the current baseline before modifying anything.

Do not assume something is missing until you verify the repository.

============================================================
RULE 5 — READ EXISTING TEST DESIGN
============================================================

The human-authored test design is the quality bar.

The agent must eventually support test cases structured around:

- Test Case ID
- User Story Reference
- Scenario
- Description
- Preconditions
- Steps
- Test Data
- Expected Result
- Test Type
- Priority
- Actual Result
- Status
- Remarks

Also preserve the richer existing internal structures if already available:

- preconditions
- test_data
- actor_role
- acceptance_criteria
- final_assertions
- risk_level

Do not throw away the existing domain model.

Extend it where necessary.

============================================================
P0 — CORE CORRECTNESS
============================================================

P0 means:

THE SYSTEM CANNOT BE CONSIDERED FUNCTIONALLY COMPLETE UNTIL THESE WORK.

------------------------------------------------------------
P0.1 REAL TEST CASE IMPORT
------------------------------------------------------------

Implement support for ingesting the existing human-authored test case format.

Inspect the repository first.

If no XLSX importer exists, implement one using the project's existing dependency strategy.

Do NOT invent a new test-case format.

The importer must map the human test-case structure into the existing domain model.

Expected conceptual mapping:

Excel Test Case
        ↓
StorySpecification / GeneratedTestCase
        ↓
Preconditions
Test Data
Ordered Steps
Expected Results
Test Type
Priority
Acceptance Criteria
Final Assertions

The imported test case must retain:

- original test case ID
- story reference
- scenario
- description
- preconditions
- steps
- test data
- expected result
- test type
- priority

No information may silently disappear during import.

Add automated tests for:

- valid workbook
- missing required columns
- malformed row
- empty test case
- multiple test cases
- duplicate test case IDs
- unsupported test type
- missing expected result

------------------------------------------------------------
P0.2 SCRIPTED TEST EXECUTION MODE
------------------------------------------------------------

When a test case already contains explicit ordered steps:

DO NOT ask the LLM to reinvent the step.

For example:

Human test case:

    Step 1:
    Open INC0000007

    Step 2:
    Verify State = On Hold

    Step 3:
    Verify Hold Reason = Awaiting Caller

    Step 4:
    Change State to In Progress

    Step 5:
    Click Update

    Step 6:
    Re-open INC0000007

    Step 7:
    Verify State = In Progress

The agent must execute those steps.

Do NOT transform them into a completely new autonomous plan.

The existing perception architecture MUST still operate underneath:

    Step
      ↓
    DOM
      ↓
    Moondream if DOM insufficient
      ↓
    Gemini if Moondream fails
      ↓
    ActionPolicy
      ↓
    Playwright
      ↓
    Verification
      ↓
    Validation

The difference is:

PLANNING is skipped when the human has already supplied the executable test intent.

This reduces unnecessary LLM calls and increases reproducibility.

------------------------------------------------------------
P0.3 STEP → ACTION CACHE
------------------------------------------------------------

Extend the existing LearningService / PageStateFingerprint mechanism.

Current learning/recovery must NOT be bypassed.

Add a safe cache concept:

    page_state_fingerprint
    +
    normalized_test_step
    +
    application/context
        ↓
    previously verified action/locator

Example:

    fingerprint = ABC123
    step = "Click Update"
        ↓
    verified selector = #sysverb_update

On the next identical execution:

    same page fingerprint
    +
    same step
        ↓
    reuse verified action

IMPORTANT:

The cache MUST NOT blindly trust stale selectors.

Before executing cached actions:

- verify page context
- verify selector still exists
- verify visibility/executability
- invalidate stale cache entries
- fall back to DOM
- then Moondream
- then Gemini

The cache is an optimization.

It must NEVER bypass safety or perception validation.

------------------------------------------------------------
P0.4 INCIDENT LIFECYCLE — PRIMARY ACCEPTANCE TEST
------------------------------------------------------------

The primary real-world acceptance test is:

INC0000007

Initial conditions:

    Incident Number:
    INC0000007

    State:
    On Hold

    Hold Reason:
    Awaiting Caller

Required actions:

    Change State → In Progress

    Click Update

Then:

    Re-open INC0000007

Final condition:

    Incident Number = INC0000007

    State = In Progress

DO NOT:

- create another incident
- close the incident
- modify unrelated fields
- change priority
- change assignment
- change description
- change caller
- change category
- execute unrelated setup actions

The test must independently verify the final persisted state.

Do not infer persistence from the pre-update page.

The agent MUST:

    open record
      ↓
    verify record identity
      ↓
    verify initial state
      ↓
    verify hold reason
      ↓
    modify state
      ↓
    update
      ↓
    reopen same record
      ↓
    independently read persisted state
      ↓
    verify In Progress

If reopening fails:

DO NOT mark PASS.

Trace and fix the navigation/root cause.

------------------------------------------------------------
P0.5 NAVIGATION IDENTITY SAFETY
------------------------------------------------------------

The previous system encountered invalid ServiceNow relative URLs.

Investigate all ServiceNow URL construction.

Never blindly call:

    page.goto(relative_url)

if Playwright requires an absolute URL.

Use the existing browser/base URL architecture.

Prefer stable record identity:

    sys_id
    incident number

over fragile list/navigation URLs whenever possible.

Verify that:

    INC0000007
        ↓
    correct sys_id
        ↓
    correct incident record

is maintained throughout the workflow.

Add regression tests around:

- absolute ServiceNow URL
- relative URL resolution
- record URL generation
- incident list URL
- incident form URL
- malformed URL
- wrong/missing base URL

------------------------------------------------------------
P0.6 FINAL VERIFICATION
------------------------------------------------------------

Final verification MUST be independent.

The final result cannot be:

    "we clicked Update successfully"

It must be:

    "we reopened INC0000007 and observed State = In Progress"

The verification must operate on fresh browser state.

If possible:

    reload/reopen
        ↓
    observe
        ↓
    verify

Do not use cached state for final assertions.

------------------------------------------------------------
P0.7 FALSE-PASS PREVENTION
------------------------------------------------------------

The following are NOT sufficient for PASS:

- state machine reached COMPLETED
- actions > 0
- defects = 0
- browser click succeeded
- no exception occurred

PASS requires:

    required steps executed
    +
    expected results verified
    +
    final assertions verified
    +
    no unresolved defects
    +
    evidence exists

If any required assertion is missing:

    NOT PASS

------------------------------------------------------------
P0.8 FINDING / DEFECT CONSISTENCY
------------------------------------------------------------

Ensure:

    Run.defect_count
    Run.findings_count
    Finding records
    TestReport
    UI

all agree.

For a genuine successful acceptance test:

    defects = 0
    findings = 0

If there is a real defect:

    persist it

Do not manipulate counters.

Deduplicate identical underlying failures.

============================================================
P1 — HIGH PRIORITY PERFORMANCE / RELIABILITY
============================================================

After P0 is stable, implement P1.

------------------------------------------------------------
P1.1 LLM RATE LIMITING
------------------------------------------------------------

Inspect the existing global LLM rate limiter.

If all providers/purposes currently share one global lock:

split logically by provider/purpose.

At minimum distinguish:

    planner
    Moondream
    Gemini
    verification

Do NOT violate provider limits.

Goal:

An unrelated planner call should not unnecessarily block a vision call.

Measure before/after latency.

------------------------------------------------------------
P1.2 SCREENSHOT DEDUPLICATION
------------------------------------------------------------

Inspect the current screenshot lifecycle.

Avoid unnecessary sequences such as:

    before_perception
    vision screenshot
    escalation screenshot
    after_perception

when the browser state has not changed.

Reuse the same screenshot where safe.

IMPORTANT:

Never reuse an image after the browser state has materially changed.

Evidence correctness is more important than saving one screenshot.

------------------------------------------------------------
P1.3 VISION CALL BUDGET
------------------------------------------------------------

Introduce an action-level model call budget.

A failed action must not explode into:

    DOM
    → Moondream
    → Gemini
    → retry
    → Moondream
    → Gemini
    → verifier
    → recovery
    → verifier
    ...

Track:

    planner calls
    Moondream calls
    Gemini calls
    verification calls

per action and per run.

Expose this telemetry in logs/reporting.

Do not silently skip required verification.

------------------------------------------------------------
P1.4 PAGE-STATE GROUNDING CACHE
------------------------------------------------------------

Extend PageStateFingerprint to cache successful first-pass grounding.

Example:

    fingerprint X
    target "Update"
        ↓
    DOM selector #sysverb_update

Next occurrence:

    fingerprint X
    target "Update"
        ↓
    reuse verified grounding

If fingerprint changes materially:

    invalidate/re-evaluate

This must remain compatible with:

    DOM → Moondream → Gemini

------------------------------------------------------------
P1.5 PROMPT EFFICIENCY
------------------------------------------------------------

Inspect repeated system prompts.

Where provider APIs support caching, use the existing supported mechanism.

Do not implement fake caching.

Reduce repeated static context where safely possible.

Do not remove important instructions just to reduce tokens.

------------------------------------------------------------
P1.6 CELERY CONCURRENCY
------------------------------------------------------------

Inspect current worker configuration.

If currently:

    --pool=solo

determine whether the runtime environment permits a safer concurrent configuration.

Do not blindly change it.

Test worker behavior first.

If concurrency is introduced:

- make it configurable
- ensure browser sessions remain isolated
- ensure one UAT run cannot affect another
- ensure database state is safe
- ensure logs remain attributable to the correct run

Do not sacrifice browser isolation for throughput.

------------------------------------------------------------
P1.7 BROWSER WARMING
------------------------------------------------------------

Investigate browser startup cost.

If safely possible within existing architecture:

reuse browser process/context where appropriate.

DO NOT share state between unrelated UAT sessions.

Authentication/session isolation must remain intact.

============================================================
P1.8 TEST DESIGN COVERAGE
============================================================

The human test design includes more than happy-path tests.

Extend the existing testing strategy taxonomy to support:

    Functional
    Boundary
    Equivalence
    Decision Table
    State Transition
    Security / RBAC
    Idempotency
    Audit / Logging
    Error Isolation
    End-to-End

Do not remove existing strategies.

Add a `test_type` or equivalent field only if the current model requires it.

Ensure the planner/importer can preserve the test type.

============================================================
P1.9 ROLE / PERSONA EXECUTION
============================================================

Inspect existing actor_role support.

Implement role-sweep capability only if it fits the existing architecture.

Example:

    Test Case
       ↓
    PM
       ↓
    Program Manager
       ↓
    PMO

For each role:

- authenticate correctly
- execute the same relevant test
- verify expected visibility/editability
- isolate browser/session state
- report results separately

Do not assume every test needs every role.

Role execution must be explicitly defined by the test case.

============================================================
P1.10 XLSX RESULT EXPORT
============================================================

The human tester works in a structured spreadsheet.

The agent must eventually be able to export execution results back into the same conceptual schema:

    Test Case ID
    User Story Ref
    Scenario
    Description
    Preconditions
    Steps
    Test Data
    Expected Result
    Test Type
    Priority
    Actual Result
    Status
    Remarks

Actual Result must contain factual execution information.

Example:

    "Incident INC0000007 was reopened after Update.
     State was observed as In Progress."

Do not output vague statements such as:

    "Test completed successfully."

============================================================
P2 — PRODUCTION HARDENING
============================================================

------------------------------------------------------------
P2.1 SECURITY CONFIGURATION
------------------------------------------------------------

Inspect JWT configuration.

If a well-known default JWT secret can be used outside local development:

fix this.

Non-local/production runtime must require an explicit secret.

Do not break intentional local development behavior.

Add startup validation.

------------------------------------------------------------
P2.2 DOCKER HARDENING
------------------------------------------------------------

Inspect Dockerfiles.

Where safe:

- non-root runtime user
- multi-stage build
- remove unnecessary build dependencies
- healthcheck
- explicit runtime configuration

Do not modify Docker architecture unnecessarily.

Run actual containers after changes.

Inspect:

    docker compose ps

and relevant logs.

------------------------------------------------------------
P2.3 OBSERVABILITY
------------------------------------------------------------

Every action should be traceable.

A useful execution chain should look conceptually like:

    run_id
      ↓
    step_id
      ↓
    action_id
      ↓
    perception
      ↓
    execution
      ↓
    verification
      ↓
    validation
      ↓
    evidence
      ↓
    finding

Use existing logging conventions where possible.

Do not create an entirely new logging architecture.

------------------------------------------------------------
P2.4 MODEL FAILURE VS PRODUCT FAILURE
------------------------------------------------------------

Separate:

    NVIDIA API failure
    Moondream failure
    Gemini failure
    browser failure
    ServiceNow application defect
    validation failure

Example:

NVIDIA returns HTTP 503.

That is NOT automatically:

    ServiceNow defect.

Likewise:

Playwright selector fails.

That is not automatically:

    ServiceNow application defect.

The reporting layer must distinguish infrastructure/model/browser problems from actual UAT defects.

------------------------------------------------------------
P2.5 NETWORK DELTA VALIDATION
------------------------------------------------------------

Apply the baseline/delta principle used for console errors.

Avoid treating unrelated pre-existing network issues as defects.

Detect genuinely new failures caused by the current action where possible.

------------------------------------------------------------
P2.6 LEARNING SAFETY
------------------------------------------------------------

Ensure learned recovery never becomes an unsafe shortcut.

Required order remains:

    DOM
      ↓
    Moondream
      ↓
    Gemini
      ↓
    learned recovery where appropriate

or the equivalent existing safe recovery design.

A learned selector must be executable and context-valid before use.

============================================================
P2.7 PERCEPTION EVIDENCE
============================================================

Ensure the actual runtime perception information survives:

    perception
       ↓
    ActionResult
       ↓
    StepEvidence
       ↓
    TestReport
       ↓
    persistence
       ↓
    API
       ↓
    UI

If DOM was used:

    route = DOM

If Moondream was used:

    provider = Moondream

If Gemini was used:

    provider = Gemini

Never fabricate perception records.

============================================================
P2.8 KNOWLEDGE MODEL
============================================================

Inspect the existing Knowledge Model implementation.

Verify whether:

    GET /api/v1/knowledge-model/rules
    GET /api/v1/knowledge-model/rules/{rule_id}
    GET /api/v1/knowledge-model/drift

are implemented and correctly connected to the existing CustomerKnowledgeModel.

If already implemented:

fix the integration/UI issue.

If genuinely missing:

implement the smallest compatible API.

Do not redesign the Knowledge Model.

Do not fabricate rules.

============================================================
P2.9 REGRESSION TESTING
============================================================

Protect the existing baseline.

Previously established baseline:

    175 passed
    7 skipped
    0 failed

If current repository has a different baseline, record the actual baseline first.

After every significant change:

    targeted tests

Then:

    relevant regression suite

At the end:

    full regression suite

Never delete or weaken tests simply because they fail after your change.

============================================================
P3 — MAINTAINABILITY / QUALITY
============================================================

------------------------------------------------------------
P3.1 DOCUMENTATION
------------------------------------------------------------

Create/update:

    docs/architecture_decision_records/

Include an ADR explaining:

    Why DOM is preferred
    Why Moondream is PRIMARY vision
    Why Gemini is fallback
    Why Playwright performs execution
    Why AI does not directly control unrestricted browser actions

------------------------------------------------------------
P3.2 CONTRIBUTING
------------------------------------------------------------

Add/update:

    CONTRIBUTING.md

Explain:

- local setup
- Docker setup
- tests
- logs
- architecture
- model configuration
- safe modification rules

------------------------------------------------------------
P3.3 TROUBLESHOOTING RUNBOOK
------------------------------------------------------------

Create:

    docs/troubleshooting.md

Cover:

    Docker failure
    browser startup failure
    Playwright selector failure
    Moondream failure
    Gemini fallback
    NVIDIA 503
    validation failure
    evidence missing
    findings mismatch
    ServiceNow navigation failure
    authentication failure

------------------------------------------------------------
P3.4 ROOT REPOSITORY HYGIENE
------------------------------------------------------------

Inspect tracked files.

Runtime artifacts such as:

    api.log
    worker.log

must not be committed.

Diagnostic scripts should be organized appropriately.

Do NOT blindly rewrite Git history.

If history rewriting would be destructive:

STOP and document the issue rather than performing a dangerous history rewrite.

============================================================
P3.5 ACTUAL RESULT NARRATIVE
============================================================

Generate a human-readable Actual Result only once per completed test case where possible.

Do not make every step invoke an LLM just to generate prose.

Actual Result should be factual.

Example:

    "INC0000007 was initially observed in On Hold with
     Hold Reason Awaiting Caller. The State was changed to
     In Progress and Update was executed. The incident was
     reopened and independently verified as In Progress."

============================================================
P3.6 COST / PERFORMANCE TELEMETRY
============================================================

Track per run:

    total actions
    DOM actions
    Moondream calls
    Gemini calls
    planner calls
    verification calls
    screenshots
    average action latency
    model latency
    total runtime

This allows future optimization based on measurements rather than guesses.

============================================================
AUTONOMOUS EXECUTION LOOP
============================================================

You are explicitly authorized to iterate.

For every discovered issue:

1. Identify symptom.
2. Find exact code path.
3. Find root cause.
4. Implement smallest correct fix.
5. Add/adjust regression test.
6. Run targeted test.
7. Run relevant integration test.
8. If runtime behavior matters:
       run the actual system.
9. Inspect logs yourself.
10. Compare actual behavior against acceptance criteria.
11. If anything fails:
       continue.
12. Repeat.

DO NOT stop merely because one layer works.

============================================================
SELF-ROAST PROTOCOL
============================================================

Before declaring anything fixed, aggressively challenge your own result.

Ask:

    Did I actually test the runtime?

    Did I only inspect the code?

    Did Playwright really perform the action?

    Did ServiceNow actually persist the change?

    Did I reopen the same record?

    Did I independently verify the final state?

    Did the expected result come from the test case?

    Did I accidentally let the planner invent extra actions?

    Did I bypass Moondream?

    Did Gemini become primary accidentally?

    Did I weaken validation?

    Did I suppress an exception?

    Did I manipulate defect counters?

    Did I make the report look better instead of making
    the system better?

    Did the UI receive real evidence?

    Could this still be a false PASS?

If the answer to any question is uncertain:

    CONTINUE.

============================================================
FINAL ACCEPTANCE TEST
============================================================

The following must be executed against the actual ServiceNow environment.

TEST:

    INC0000007 Incident Lifecycle

PRECONDITION:

    Incident exists.

INITIAL ASSERTIONS:

    Incident Number = INC0000007
    State = On Hold
    Hold Reason = Awaiting Caller

ACTION:

    Change State to In Progress
    Click Update

POST-ACTION:

    Reopen INC0000007

FINAL ASSERTIONS:

    Incident Number = INC0000007
    State = In Progress

NEGATIVE ASSERTIONS:

    No unrelated fields modified.
    No new incident created.
    Incident not closed.
    Wrong record not opened.
    No unrelated actions executed.

PERCEPTION ASSERTIONS:

    DOM used when reliable.
    Moondream used when DOM insufficient.
    Gemini only used as fallback where necessary.

VALIDATION ASSERTIONS:

    Browser action succeeded.
    Expected state transition occurred.
    Final state independently verified.
    No unexpected new JS errors.
    No unexpected action-caused network failures.
    Behavioral verification passed or valid deterministic fallback passed.

REPORTING ASSERTIONS:

    action count correct.
    finding count correct.
    defect count correct.
    evidence persisted.
    perception evidence persisted where applicable.
    final report matches actual execution.

============================================================
FINAL REGRESSION GATE
============================================================

Run the complete test suite.

Record exact:

    collected
    passed
    skipped
    failed

No unexplained regression is acceptable.

============================================================
FINAL MASTER REPORT
============================================================

Create/update ONE report:

    docs/final_autonomous_stabilization_report.md

Do not create dozens of scattered reports.

The report MUST contain:

1. Executive Summary

2. Repository Baseline

3. Current Architecture

4. P0 Work

5. P1 Work

6. P2 Work

7. P3 Work

8. Every Issue Discovered

For every issue:

    symptom
    log evidence
    root cause
    affected files
    fix
    tests
    result

9. Runtime Iteration History

    Iteration
    Problem
    Root Cause
    Fix
    Test
    Result

10. Incident Acceptance Evidence

    INC0000007
    Initial State
    Initial Hold Reason
    State Change
    Update
    Reopen
    Final State

11. Perception Evidence

    DOM
    Moondream
    Gemini

including actual confidence/coordinates/locators where available.

12. Model Usage

    Nemotron calls
    Moondream calls
    Gemini calls
    verification calls

13. Performance

    runtime
    action latency
    model latency
    screenshot count

14. Regression Results

15. Remaining Issues

16. Security Findings

17. Final Verdict

Use EXACTLY:

    PASS — ALL ACCEPTANCE CRITERIA VERIFIED

or:

    FAIL — ACCEPTANCE CRITERIA NOT FULLY VERIFIED

Never use:

    "mostly passed"
    "almost done"
    "looks good"
    "production ready"

unless the evidence genuinely supports it.

============================================================
ORDER OF EXECUTION
============================================================

Do NOT attack everything randomly.

Execute in this order:

PHASE 0
Repository inspection + baseline

        ↓

PHASE 1
P0 security/correctness blockers

        ↓

PHASE 2
Real test-case import

        ↓

PHASE 3
Scripted execution mode

        ↓

PHASE 4
INC0000007 end-to-end lifecycle

        ↓

PHASE 5
Final verification + evidence + findings

        ↓

PHASE 6
P1 performance/cost/reliability

        ↓

PHASE 7
P2 production hardening

        ↓

PHASE 8
P3 documentation/product quality

        ↓

PHASE 9
Full regression suite

        ↓

PHASE 10
Repeat failed areas until green

        ↓

FINAL MASTER REPORT

============================================================
ULTIMATE RULE
============================================================

DO NOT STOP UNTIL THE SYSTEM HAS ACTUALLY DEMONSTRATED THE TARGET.

The target is NOT:

"the code looks correct."

The target is NOT:

"tests passed."

The target is NOT:

"the UI says completed."

The target is:

    HUMAN TEST CASE
          ↓
    IMPORTED CORRECTLY
          ↓
    EXECUTED CORRECTLY
          ↓
    SERVICE NOW ACTUALLY CHANGED
          ↓
    RECORD REOPENED
          ↓
    FINAL STATE INDEPENDENTLY VERIFIED
          ↓
    EVIDENCE PERSISTED
          ↓
    FINDINGS CORRECT
          ↓
    REPORT CORRECT
          ↓
    REGRESSION SUITE GREEN
          ↓
    GENUINE PASS

Be surgical.

Be skeptical.

Be autonomous.

Read your own logs.

Fix your own mistakes.

Never hide a failure.

Never cheat the acceptance criteria.

Never change the architecture simply because an implementation is difficult.

The existing Hybrid Browser Agent architecture is the product.

Your job is to make it reliable.


</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-15T12:54:05+05:30.
</ADDITIONAL_METADATA>
<USER_SETTINGS_CHANGE>
The user changed setting `Model Selection` from Gemini 3.8 Flash (High) to Gemini 3.1 Pro (High). No need to comment on this change if the user doesn't ask about it. If reporting what model you are, please use a human readable name instead of the exact string.
</USER_SETTINGS_CHANGE>